<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Online Exam</title>
    <style>
        /* Reset some default styles */
body, h1, h2, ul {
    margin: 0;
    padding: 0;
}

/* Basic styling */
body {
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
    line-height: 1.6;
}

header{
    background-color: #fff;
    color: black;
    padding: 20px;
    text-align: center;
}
 h1{
    display: inline-block;
 }

 a {
  background-color:whitesmoke;
  color: black;
  padding: 14px 18px;
  text-align: center;
  text-decoration: none;
  margin-left: 30px;
  border: 1px solid black;
  font-size: 15px;
  border-radius: 5px;
  float: right;
}
 a:hover{
      background-color: rgb(230, 221, 221);
  }

main {
    max-width: 1200px;
    margin: 20px auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

 /* Add this to your styles.css */

.book-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
    margin-top: 20px;
}

.book-card {
    background-color: #fff;
    padding: 20px;
    box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
    text-align: center;
}

.book-card img {
    margin-bottom: 10px;
    width:  200px;
    height: 300px;
    object-fit: cover;
}

.book-card h3 {
    font-size: 1.2rem;
}

.book-card button {
    background-color: #007bff;
    color: #fff;
    border: none;
    padding: 5px 10px;
    cursor: pointer;
    transition: background-color 0.3s;
}

.book-card button:hover {
    background-color: #0056b3;
}


footer {
    text-align: center;
    padding: 10px;
    background-color: #333;
    color: #fff;
}

/* Add more styles for book cards, buttons, etc. here */

    </style>

</head>
<body>
    <header>
        <h1>Online Exam Portal </h1>
        <a href="http://localhost:3000/registration" >Register</a>
        <a href="http://localhost:3000/login">Login</a> 
    </header>
    <main>
      <h2>You can buy these books to start preparation for exam</h2>
    <section class="book-list">
       {{#each books}}
        <article class="book-card">
            <img src={{this.image}} alt="Image of book">
            <h3>{{this.title}}</h3>
            <p>Author: {{this.author}}</p>
            <p>Price: Rs {{this.price}}</p>
            <button>Click to buy</button>
        </article>
        {{/each}}

        <!-- Add more book cards here -->
    </section>
</main>

    <footer>
        <p>&copy; 2023 Online Exam Portal</p>
    </footer>
</body>
</html>
